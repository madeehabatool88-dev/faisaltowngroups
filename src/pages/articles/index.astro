---
import ContentLayout from "../../layouts/ContentLayout.astro";
import { articles } from "../../data/articles";
---
<ContentLayout
  title="Faisal Town Phase 2 Master Plan Guide"
  description="Open the focused Faisal Town Phase 2 master plan guide for sector layout, roads and plot-location checks."
  eyebrow="Master Plan"
  heading="Faisal Town Phase 2 map guide"
  subheading="One focused guide for understanding the Phase 2 layout before moving into the correct sector page."
>
  <section class="content-section">
    <div class="article-grid compact">
      {articles.map((article) => (
        <article class="article-card">
          <img src={article.image} alt={article.title} loading="lazy" />
          <div>
            <span>{article.category} · {article.readTime}</span>
            <h2><a href={`/articles/${article.slug}/`}>{article.title}</a></h2>
            <p>{article.description}</p>
            <a class="text-link" href={`/articles/${article.slug}/`}>Open Master Plan Guide</a>
          </div>
        </article>
      ))}
    </div>
  </section>
</ContentLayout>
